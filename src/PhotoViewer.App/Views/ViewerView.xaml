<Window x:Class="PhotoViewer.App.Views.ViewerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhotoViewer.App.Views"
        xmlns:controls="clr-namespace:PhotoViewer.App.Controls"
        mc:Ignorable="d"
        Title="LuminaView - 圖片檢視器"
        Height="768"
        Width="1024"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}"
        KeyDown="Window_KeyDown"
        Topmost="True">

    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="0.9" To="1" Duration="0:0:0.3">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="0.9" To="1" Duration="0:0:0.3">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Window.RenderTransformOrigin>0.5,0.5</Window.RenderTransformOrigin>
    <Window.RenderTransform>
        <ScaleTransform ScaleX="1" ScaleY="1"/>
    </Window.RenderTransform>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 頂部工具列 -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="10,8">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="PreviousButton"
                        Padding="15,8"
                        Margin="0,0,5,0"
                        Click="PreviousButton_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        ToolTip="上一張">
                    <Path Data="{StaticResource Icon_Previous}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="16" Height="16" Stretch="Uniform"/>
                </Button>

                <Button x:Name="NextButton"
                        Padding="15,8"
                        Margin="0,0,20,0"
                        Click="NextButton_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        ToolTip="下一張">
                    <Path Data="{StaticResource Icon_Next}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="16" Height="16" Stretch="Uniform"/>
                </Button>

                <Button x:Name="SlideshowButton"
                        Padding="15,8"
                        Margin="0,0,20,0"
                        Click="SlideshowButton_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        ToolTip="幻燈片播放 (F5)">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource Icon_Play}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,8,0"/>
                        <TextBlock Text="播放"/>
                    </StackPanel>
                </Button>

                <Button x:Name="FitButton"
                        Padding="12,8"
                        Margin="0,0,5,0"
                        Click="FitButton_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        ToolTip="適應視窗">
                     <Path Data="{StaticResource Icon_Fit}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="16" Height="16" Stretch="Uniform"/>
                </Button>

                <Button x:Name="ActualSizeButton"
                        Padding="12,8"
                        Margin="0,0,5,0"
                        Click="ActualSizeButton_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        ToolTip="實際大小">
                    <Path Data="{StaticResource Icon_Original}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="16" Height="16" Stretch="Uniform"/>
                </Button>

                <Button x:Name="RotateLeftButton"
                        Padding="12,8"
                        Margin="0,0,5,0"
                        Click="RotateLeftButton_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        ToolTip="左轉">
                    <Path Data="{StaticResource Icon_RotateLeft}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="16" Height="16" Stretch="Uniform"/>
                </Button>

                <Button x:Name="RotateRightButton"
                        Padding="12,8"
                        Margin="0,0,5,0"
                        Click="RotateRightButton_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        ToolTip="右轉">
                    <Path Data="{StaticResource Icon_RotateRight}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="16" Height="16" Stretch="Uniform"/>
                </Button>

                <Button x:Name="InfoButton"
                        Padding="12,8"
                        Margin="0,0,20,0"
                        Click="InfoButton_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        ToolTip="資訊">
                    <Path Data="{StaticResource Icon_Info}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="16" Height="16" Stretch="Uniform"/>
                </Button>

                <TextBlock x:Name="ImageInfoTextBlock"
                          Text=""
                          VerticalAlignment="Center"
                          Foreground="{DynamicResource SecondaryTextBrush}"
                          FontSize="13"
                          Margin="10,0"/>
            </StackPanel>
        </Border>

        <!-- 主內容區域 - 使用 Grid 讓圖片和資訊面板並排 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition x:Name="InfoPanelColumn" Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- SkiaSharp 畫布 -->
            <controls:SkiaCanvasControl x:Name="ImageCanvas" Grid.Column="0"/>

            <!-- EXIF Info Panel -->
            <Border x:Name="InfoPanel"
                    Grid.Column="1"
                    Width="320"
                    Background="{DynamicResource SurfaceBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1,0,0,0"
                    Visibility="Collapsed">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="-5" BlurRadius="20" Opacity="0.4"/>
                </Border.Effect>
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="圖片資訊" 
                                   FontSize="18" 
                                   FontWeight="Light" 
                                   Foreground="{DynamicResource PrimaryTextBrush}"/>
                        <Rectangle Height="1" Fill="{DynamicResource BorderBrush}" Margin="0,10,0,0"/>
                    </StackPanel>

                    <!-- 主要 EXIF 資訊 -->
                    <StackPanel Grid.Row="1" Margin="0,0,0,15">
                        <TextBlock x:Name="ExifCameraText" 
                                   Text="" 
                                   FontSize="14" 
                                   FontWeight="SemiBold" 
                                   Foreground="{DynamicResource PrimaryTextBrush}" 
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,5"/>
                        
                        <TextBlock x:Name="ExifLensText" 
                                   Text="" 
                                   FontSize="12" 
                                   Foreground="{DynamicResource SecondaryTextBrush}" 
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,10"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="ISO" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock x:Name="ExifIsoText" Text="-" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="光圈" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock x:Name="ExifApertureText" Text="-" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="快門" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock x:Name="ExifShutterText" Text="-" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                             <TextBlock Text="焦距: " FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"/>
                             <TextBlock x:Name="ExifFocalLengthText" Text="-" FontSize="11" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal">
                             <TextBlock Text="拍攝時間: " FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"/>
                             <TextBlock x:Name="ExifDateText" Text="-" FontSize="11" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <TextBlock Grid.Row="2" Text="詳細資訊" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,10,0,5"/>

                    <ListView x:Name="ExifListView" 
                              Grid.Row="3"
                              Background="Transparent" 
                              BorderThickness="0" 
                              Foreground="{DynamicResource PrimaryTextBrush}"
                              FontSize="11">
                        <ListView.Resources>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="2"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="Padding" Value="4,2"/>
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </Style>
                        </ListView.Resources>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Property" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Key}" 
                                                       Foreground="{DynamicResource SecondaryTextBrush}" 
                                                       FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Value" Width="180">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Value}" 
                                                       TextWrapping="Wrap"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>
        </Grid>

        <!-- 底部狀態列 -->
        <Border Grid.Row="2" Background="{DynamicResource AccentBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="10,6">
            <Grid>
                <TextBlock x:Name="FileNameTextBlock"
                          Text=""
                          VerticalAlignment="Center"
                          Foreground="White"
                          FontSize="12"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock x:Name="ZoomTextBlock"
                              Text="100%"
                              VerticalAlignment="Center"
                              Foreground="White"
                              FontSize="11"
                              Margin="0,0,15,0"/>
                    <TextBlock x:Name="PositionTextBlock"
                              Text="1 / 1"
                              VerticalAlignment="Center"
                              Foreground="White"
                              FontSize="11"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 載入指示器 -->
        <Grid x:Name="LoadingPanel"
              Grid.Row="1"
              Background="#80000000"
              Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⏳"
                          FontSize="48"
                          HorizontalAlignment="Center"
                          Foreground="White"/>
                <TextBlock Text="載入中..."
                          FontSize="16"
                          Foreground="White"
                          HorizontalAlignment="Center"
                          Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
