<Window x:Class="PhotoViewer.App.Views.ViewerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhotoViewer.App.Views"
        xmlns:controls="clr-namespace:PhotoViewer.App.Controls"
        mc:Ignorable="d"
        Title="LuminaView - 圖片檢視器"
        Height="768"
        Width="1024"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E1E"
        KeyDown="Window_KeyDown"
        Topmost="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 頂部工具列 -->
        <Border Grid.Row="0" Background="#FF3E3E42" BorderBrush="#FF555555" BorderThickness="0,0,0,1" Padding="10,8">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="PreviousButton"
                        Content="◀ 上一張"
                        Padding="15,8"
                        Margin="0,0,5,0"
                        Click="PreviousButton_Click"
                        Style="{StaticResource ModernButtonStyle}"/>

                <Button x:Name="NextButton"
                        Content="下一張 ▶"
                        Padding="15,8"
                        Margin="0,0,20,0"
                        Click="NextButton_Click"
                        Style="{StaticResource ModernButtonStyle}"/>

                <Button x:Name="FitButton"
                        Content="🔲 適應視窗"
                        Padding="12,8"
                        Margin="0,0,5,0"
                        Click="FitButton_Click"
                        Style="{StaticResource ModernButtonStyle}"/>

                <Button x:Name="ActualSizeButton"
                        Content="⊡ 實際大小"
                        Padding="12,8"
                        Margin="0,0,5,0"
                        Click="ActualSizeButton_Click"
                        Style="{StaticResource ModernButtonStyle}"/>

                <Button x:Name="RotateLeftButton"
                        Content="↶ 左轉"
                        Padding="12,8"
                        Margin="0,0,5,0"
                        Click="RotateLeftButton_Click"
                        Style="{StaticResource ModernButtonStyle}"/>

                <Button x:Name="RotateRightButton"
                        Content="↷ 右轉"
                        Padding="12,8"
                        Margin="0,0,20,0"
                        Click="RotateRightButton_Click"
                        Style="{StaticResource ModernButtonStyle}"/>

                <TextBlock x:Name="ImageInfoTextBlock"
                          Text=""
                          VerticalAlignment="Center"
                          Foreground="#FFCCCCCC"
                          FontSize="13"
                          Margin="10,0"/>
            </StackPanel>
        </Border>

        <!-- SkiaSharp 畫布 -->
        <controls:SkiaCanvasControl x:Name="ImageCanvas"
                                    Grid.Row="1"/>

        <!-- 底部狀態列 -->
        <Border Grid.Row="2" Background="#FF007ACC" BorderBrush="#FF555555" BorderThickness="0,1,0,0" Padding="10,6">
            <Grid>
                <TextBlock x:Name="FileNameTextBlock"
                          Text=""
                          VerticalAlignment="Center"
                          Foreground="White"
                          FontSize="12"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock x:Name="ZoomTextBlock"
                              Text="100%"
                              VerticalAlignment="Center"
                              Foreground="White"
                              FontSize="11"
                              Margin="0,0,15,0"/>
                    <TextBlock x:Name="PositionTextBlock"
                              Text="1 / 1"
                              VerticalAlignment="Center"
                              Foreground="White"
                              FontSize="11"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 載入指示器 -->
        <Grid x:Name="LoadingPanel"
              Grid.Row="1"
              Background="#80000000"
              Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⏳"
                          FontSize="48"
                          HorizontalAlignment="Center"
                          Foreground="White"/>
                <TextBlock Text="載入中..."
                          FontSize="16"
                          Foreground="White"
                          HorizontalAlignment="Center"
                          Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
