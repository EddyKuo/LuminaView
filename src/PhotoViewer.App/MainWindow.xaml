<Window x:Class="PhotoViewer.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhotoViewer.App"
        xmlns:util="clr-namespace:PhotoViewer.App.Utilities"
        xmlns:controls="clr-namespace:PhotoViewer.App.Controls"
        xmlns:models="clr-namespace:PhotoViewer.Core.Models;assembly=PhotoViewer.Core"
        mc:Ignorable="d"
        Title="⚡ LUMINA VIEW - Cyberpunk Photo Viewer"
        Height="768"
        Width="1280"
        WindowState="Maximized"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 頂部工具列 - Cyberpunk Style -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBackgroundBrush}" BorderThickness="0,0,0,2" Padding="15,12">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#00F0FF" Offset="0"/>
                    <GradientStop Color="#BB00FF" Offset="0.5"/>
                    <GradientStop Color="#FF006E" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.Effect>
                <DropShadowEffect Color="#00F0FF" ShadowDepth="0" BlurRadius="15" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <Button x:Name="OpenFolderButton"
                        Content="📂 開啟檔案夾"
                        Padding="18,10"
                        Margin="0,0,15,0"
                        Click="OpenFolderButton_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        FontWeight="SemiBold"
                        FontSize="13"/>

                <Border Width="1" Background="{DynamicResource BorderBrush}" Margin="10,0"/>

                <TextBlock x:Name="FolderPathTextBlock"
                          Text="尚未選擇檔案夾"
                          VerticalAlignment="Center"
                          Foreground="{DynamicResource SecondaryTextBrush}"
                          FontSize="13"
                          Margin="15,0"/>

                <TextBlock x:Name="ImageCountTextBlock"
                          Text=""
                          VerticalAlignment="Center"
                          Foreground="{DynamicResource AccentBrush}"
                          FontSize="12"
                          FontWeight="SemiBold"
                          Margin="15,0,0,0"/>

                <Button x:Name="ThemeButton"
                        Content="🌓"
                        Padding="12,8"
                        Margin="25,0,0,0"
                        Click="ThemeButton_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        ToolTip="切換深色/淺色主題"
                        FontSize="16"/>
            </StackPanel>
        </Border>

        <!-- 主內容區域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Sidebar (Folder Tree) -->
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
                <TreeView x:Name="FolderTreeView" 
                          ItemsSource="{Binding Folders}" 
                          Background="Transparent" 
                          BorderThickness="0"
                          SelectedItemChanged="FolderTreeView_SelectedItemChanged">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="{x:Type TreeViewItem}">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Padding" Value="2,4"/>
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type models:FolderNode}" ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📁" Margin="0,0,5,0" Foreground="#FFD700"/>
                                <TextBlock Text="{Binding Name}" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Border>

            <!-- 分隔條 -->
            <GridSplitter Grid.Column="1"
                         Width="5"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Stretch"
                         Background="{DynamicResource BorderBrush}"
                         ShowsPreview="True"/>

            <!-- 右側圖片網格區域 -->
            <Border Grid.Column="2">
                <Border.Background>
                    <DrawingBrush Viewport="0,0,50,50" ViewportUnits="Absolute" TileMode="Tile" Opacity="0.03">
                        <DrawingBrush.Drawing>
                            <GeometryDrawing Brush="{DynamicResource AccentBrush}">
                                <GeometryDrawing.Geometry>
                                    <RectangleGeometry Rect="0,0,50,1"/>
                                </GeometryDrawing.Geometry>
                            </GeometryDrawing>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Border.Background>
                <Grid Background="{DynamicResource WindowBackgroundBrush}">
                    <!-- 圖片列表 (虛擬化) -->
                    <ListBox x:Name="ImageListBox"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             ScrollViewer.CanContentScroll="True"
                             Padding="10">
                        
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <controls:VirtualizingWrapPanel ItemWidth="160" ItemHeight="160"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="ItemBorder"
                                                    Background="{TemplateBinding Background}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="ItemBorder" Property="Background" Value="Transparent"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="ThumbnailBorder"
                                        Width="150" Height="150" Margin="5"
                                        Background="{DynamicResource SurfaceBackgroundBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="2"
                                        CornerRadius="8"
                                        Cursor="Hand"
                                        MouseLeftButtonDown="Thumbnail_MouseLeftButtonDown"
                                        RenderTransformOrigin="0.5,0.5">
                                    <Border.RenderTransform>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    </Border.RenderTransform>
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect Color="#000000" ShadowDepth="2" BlurRadius="8" Opacity="0.25"/>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <!-- Hover Effect - Cyberpunk Neon Glow -->
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                    <Setter Property="Effect">
                                                        <Setter.Value>
                                                            <DropShadowEffect Color="#00F0FF" ShadowDepth="0" BlurRadius="25" Opacity="0.9"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                                                To="1.08" Duration="0:0:0.2">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                                                To="1.08" Duration="0:0:0.2">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                                                To="1.0" Duration="0:0:0.2">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <CubicEase EasingMode="EaseOut"/>
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                                                To="1.0" Duration="0:0:0.2">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <CubicEase EasingMode="EaseOut"/>
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                                <!-- Selection Effect - Pulsing Neon -->
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                <GradientStop Color="#150F1930" Offset="0"/>
                                                                <GradientStop Color="#300F1930" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter Property="BorderBrush" Value="{DynamicResource NeonCyanBrush}"/>
                                                    <Setter Property="BorderThickness" Value="3"/>
                                                    <Setter Property="Effect">
                                                        <Setter.Value>
                                                            <DropShadowEffect Color="#00F0FF" ShadowDepth="0" BlurRadius="30" Opacity="1.0"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <DataTrigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                                <DoubleAnimation Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.BlurRadius)"
                                                                                From="30" To="40" Duration="0:0:1.5">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <SineEase EasingMode="EaseInOut"/>
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                                <DoubleAnimation Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.Opacity)"
                                                                                From="0.8" To="1.0" Duration="0:0:1.5">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <SineEase EasingMode="EaseInOut"/>
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </DataTrigger.EnterActions>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <TextBlock Text="載入中..."
                                                  Foreground="#999999"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  FontSize="11"
                                                  FontStyle="Italic"/>

                                        <Image Stretch="Uniform" Margin="8"
                                               util:ImageLoaderBehavior.ImagePath="{Binding FilePath}"/>

                                        <!-- Selected Indicator - Cyberpunk Neon -->
                                        <Border x:Name="SelectedIndicator"
                                                Width="32" Height="32"
                                                Background="{DynamicResource NeonCyanBrush}"
                                                CornerRadius="16"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Top"
                                                Margin="0,6,6,0"
                                                Visibility="Collapsed">
                                            <Border.Effect>
                                                <DropShadowEffect Color="#00F0FF" ShadowDepth="0" BlurRadius="20" Opacity="1.0"/>
                                            </Border.Effect>
                                            <Grid>
                                                <Ellipse Stroke="{DynamicResource NeonMagentaBrush}"
                                                        StrokeThickness="2"
                                                        Margin="2"
                                                        Opacity="0.6">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect Color="#FF00FF" ShadowDepth="0" BlurRadius="10" Opacity="0.8"/>
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                                <TextBlock Text="✓"
                                                          Foreground="#0A0E27"
                                                          FontSize="18"
                                                          FontWeight="ExtraBold"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Filename Overlay -->
                                        <Border VerticalAlignment="Bottom"
                                                Background="#CC000000"
                                                CornerRadius="0,0,6,6"
                                                Padding="8,4">
                                            <TextBlock Text="{Binding FileName}"
                                                      Foreground="White"
                                                      HorizontalAlignment="Center"
                                                      FontSize="10.5"
                                                      FontWeight="Medium"
                                                      TextTrimming="CharacterEllipsis"
                                                      MaxWidth="135"/>
                                        </Border>
                                    </Grid>
                                    <Border.Triggers>
                                        <EventTrigger RoutedEvent="Border.Loaded">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                    From="0" To="1" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Border.Triggers>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                        <Setter TargetName="SelectedIndicator" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- 無圖片提示 - Cyberpunk Style -->
                    <StackPanel x:Name="EmptyStatePanel"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="Visible"
                               IsHitTestVisible="False">
                        <Border CornerRadius="10" Padding="40,30" Background="#10000000"
                                BorderThickness="2">
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#00F0FF" Offset="0"/>
                                    <GradientStop Color="#FF006E" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Border.Effect>
                                <DropShadowEffect Color="#00F0FF" ShadowDepth="0" BlurRadius="30" Opacity="0.3"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="📷"
                                          FontSize="90"
                                          HorizontalAlignment="Center"
                                          Foreground="{DynamicResource NeonCyanBrush}">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="#00F0FF" ShadowDepth="0" BlurRadius="30" Opacity="0.8"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="// NO DATA LOADED //"
                                          FontSize="18"
                                          FontWeight="Bold"
                                          FontFamily="Consolas"
                                          Foreground="{DynamicResource NeonCyanBrush}"
                                          HorizontalAlignment="Center"
                                          Margin="0,20,0,0">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="#00F0FF" ShadowDepth="0" BlurRadius="10" Opacity="0.6"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="請點擊「開啟檔案夾」選擇包含圖片的檔案夾"
                                          FontSize="14"
                                          Foreground="{DynamicResource SecondaryTextBrush}"
                                          HorizontalAlignment="Center"
                                          TextWrapping="Wrap"
                                          TextAlignment="Center"
                                          MaxWidth="400"
                                          Margin="0,15,0,0"/>
                                <Border Height="1" Background="{DynamicResource NeonPinkBrush}"
                                       Margin="50,15" Opacity="0.3">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#FF006E" ShadowDepth="0" BlurRadius="5" Opacity="0.5"/>
                                    </Border.Effect>
                                </Border>
                                <TextBlock HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="12"
                                          FontFamily="Consolas">
                                    <Run Text="SUPPORTED: " Foreground="{DynamicResource NeonPurpleBrush}" FontWeight="Bold"/>
                                    <Run Text="JPG | PNG | WebP | GIF | " Foreground="{DynamicResource SecondaryTextBrush}"/>
                                    <Run Text="RAW" Foreground="{DynamicResource NeonCyanBrush}" FontWeight="Bold"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- 底部狀態列 - Cyberpunk Gradient -->
        <Border Grid.Row="2" BorderThickness="0,2,0,0" Padding="12,8">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#FF0A0E27" Offset="0"/>
                    <GradientStop Color="#FF0F1419" Offset="0.5"/>
                    <GradientStop Color="#FF0A0E27" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#00F0FF" Offset="0"/>
                    <GradientStop Color="#FF006E" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.Effect>
                <DropShadowEffect Color="#00F0FF" ShadowDepth="0" BlurRadius="10" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="StatusTextBlock"
                              Text="⚡ READY"
                              Foreground="{DynamicResource NeonCyanBrush}"
                              FontSize="12"
                              FontWeight="Bold"
                              Margin="0,0,10,0">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#00F0FF" ShadowDepth="0" BlurRadius="8" Opacity="0.6"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    
                    <ProgressBar x:Name="LoadingProgressBar"
                                Width="100"
                                Height="12"
                                Visibility="Collapsed"
                                IsIndeterminate="False"
                                Maximum="100"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock x:Name="MemoryUsageTextBlock"
                              Text=""
                              VerticalAlignment="Center"
                              Foreground="{DynamicResource SecondaryTextBrush}"
                              FontSize="11"
                              Margin="0,0,15,0"/>
                    <TextBlock VerticalAlignment="Center" FontSize="11">
                        <Run Text="LUMINA" Foreground="{DynamicResource NeonCyanBrush}" FontWeight="Bold"/>
                        <Run Text="VIEW" Foreground="{DynamicResource NeonMagentaBrush}" FontWeight="Bold"/>
                        <Run Text=" v0.1.0" Foreground="{DynamicResource SecondaryTextBrush}"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
